type Query {
    clients: [Client]
    client(id: ID!): Client

    companies: [Company]
    company(id: ID!): Company

    projects: [Project]
    project(id: ID!): Project

    contacts: [Contact]
    contact(id: ID!): Contact

    invoices: [Invoice]
    invoice(id: ID!): Invoice

    users: [User]
    user(id: ID!): User
}
