{% extends 'base.html.twig' %}

{% block title %}Edit Client{% endblock %}

{% block body %}
<div class="edit-invoice-container">
    <h1 class="client-main-title">Edit Client</h1>
    <div class="edit-invoice-form-container">
    {{ include('client/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="edit-invoice-actions">
    <a href="{{ path('app_client_index') }}" class="edit-invoice-back-link">Back to list</a>

    {{ include('client/_delete_form.html.twig') }}
         </div>
    </div>
</div>
{% endblock %}
