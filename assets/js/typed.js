import Typed from 'typed.js';

document.addEventListener('DOMContentLoaded', () => {
    const options = {
        strings: ['Votre portail client pour une gestion simplifiée et sécurisée.'],
        typeSpeed: 50,
        backSpeed: 25,
        loop: false,
    };

    const typed = new Typed('.typed-text', options);
});